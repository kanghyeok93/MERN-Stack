{
  "name": "mern-stack-board",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "bootstrap": "^4.3.1",
    "jw-react-pagination": "^1.1.0",
    "moment": "^2.24.0",
    "node-sass": "^7.0.0",
    "promise-polyfill": "^8.1.3",
    "react": "^16.8.6",
    "react-bootstrap": "^1.0.0-beta.8",
    "react-dom": "^16.8.6",
    "react-icons": "^3.7.0",
    "react-moment": "^0.9.2",
    "react-router-dom": "^5.0.0",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "test": "react-scripts test --env=jsdom",
    "start": "cd api && node server",
    "heroku-postbuild": "npm install && npm run build",
    "build": "react-scripts build",
    "dev": "concurrently \"react-scripts start\" \"nodemon api/server.js\""
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "proxy": "http://localhost:8080",
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}
